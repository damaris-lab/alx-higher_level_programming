#!/usr/bin/python3
def best_score(a_dictionary):
    if not dictionary:
        return none
    lst = list(a_dictionary)

    max = a_dictionary[lst[0]]
    for key in lst:
        if a_dictionary[key] > max:
            max = a_dictionary[key]
            reslt = key
    return (rslt)
