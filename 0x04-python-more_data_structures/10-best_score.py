#!/usr/bin/python3
def best_score(a_dictionary):
    if not dictionary:
        return none
    lst = list(a_dictionary[lst])

    max = a_dictionary[0]
    for lst in lst:
        if a_dictionary[key] > max:
            max = a_dictionary[key]
            reslt = key
    return (rslt)
